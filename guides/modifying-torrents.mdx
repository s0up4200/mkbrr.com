# Modifying Torrents

This guide explains how to use `mkbrr modify` to update metadata in existing `.torrent` files without needing the original content.

*Content to be added based on `cmd/modify.go` help output and README examples.*
