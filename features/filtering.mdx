# Filtering Files (Include/Exclude)

Learn how to use glob patterns with the `--include` and `--exclude` flags during torrent creation to control which files are added to the torrent.

*Content to be added based on `internal/torrent/ignore.go`, `cmd/create.go` help output, and README.*
