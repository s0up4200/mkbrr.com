# Presets

Simplify torrent creation and modification by storing common settings in a `presets.yaml` file.

*Content to be added based on `internal/preset/preset.go`, `examples/presets.yaml`, `schema/presets.json`, and README.*
