# Batch Mode

Create multiple torrents simultaneously using a `batch.yaml` configuration file.

*Content to be added based on `internal/torrent/batch.go`, `examples/batch.yaml`, `schema/batch.json`, and README.*
