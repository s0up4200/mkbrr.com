# Performance Benchmarks

`mkbrr` is designed for speed. This section details the performance benchmarks comparing `mkbrr` against other popular torrent creation tools.

*Content to be added based on `README.md` performance section and `docs/benchmarks/` files.*
